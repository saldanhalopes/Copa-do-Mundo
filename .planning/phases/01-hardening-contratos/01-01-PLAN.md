# 01-01-PLAN.md — Suíte de testes Foundry com fuzzing

> Artefato GSD Core. Plano de tarefa atômico (formato `<task>`).

<task name="foundry-test-suite">

<goal>
Implementar testes Foundry para os contratos de prioridade 1 (MatchEscrow,
PackStore, RankingSeasons) com fuzzing das invariantes, atingindo ≥95% de cobertura.
</goal>

<requirements>NFR-SEC-01</requirements>

<files>
- foundry.toml (novo)
- test/foundry/MatchEscrow.t.sol (novo)
- test/foundry/PackStore.t.sol (novo)
- test/foundry/RankingSeasons.t.sol (novo)
- test/foundry/invariants/Invariants.t.sol (novo)
</files>

<steps>
1. Instalar Foundry (forge, anvil) e configurar foundry.toml apontando para contracts/
   e remappings do OpenZeppelin/Chainlink.
2. MatchEscrow.t.sol:
   - testCriarPartida: stake travado, posse verificada
   - testAceitarPartida: stake igual, não jogar contra si
   - testResolver: vencedor leva 95%, taxa vai ao tesouro
   - testFuzz_potDistribution(uint96 stake): pote == 2*stake, premio+taxa == pote
   - testCancelarTimeout: reembolso após TIMEOUT
3. PackStore.t.sol:
   - mock do VRF coordinator
   - testFuzz_rarityDistribution(uint256 seed): soma das faixas == 10000
   - testGarantiaMinima: última carta força raridade mínima
   - testLimiteDiario: bloqueia acima de LIMITE_DIARIO
4. RankingSeasons.t.sol:
   - testElo: jogadores iguais → +16/-16
   - testFuzz_eloNeverNegative(uint16 a, uint16 b): elo >= 0 sempre
   - testFinalizarTemporada: top 3 recebe 50/30/20%
5. Invariants.t.sol:
   - invariant_supplyNeverExceedsMax
   - invariant_freezeIsIrreversible
6. Rodar forge coverage e garantir ≥95% nas linhas dos contratos P1.
</steps>

<verify>
forge test -vvv && forge coverage --report summary | grep -E "MatchEscrow|PackStore|RankingSeasons"
</verify>

<done>
Todos os testes passam; cobertura ≥95% nos três contratos de prioridade 1;
todas as invariantes do CONTEXT.md cobertas por fuzzing.
</done>

</task>
